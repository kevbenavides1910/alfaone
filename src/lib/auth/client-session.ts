"use client";

/** Sesión de UI con soporte de vista previa de rol (Conectar como). */
export { useEffectiveSession as useSession } from "@/lib/impersonation/use-effective-session";
export { signOut } from "next-auth/react";
